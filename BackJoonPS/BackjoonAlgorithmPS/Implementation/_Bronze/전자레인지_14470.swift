
// MARK: - 전자레인지 14470
// MARK: swift 문제풀이

/*
var info = [Int](repeating: 0, count: 5)
for i in info.indices {
    info[i] = Int(readLine()!)!
}

var Ans = 0
if info[0] < 0 {
    Ans += (info[0] * -1) * info[2]
    Ans += info[3]
}

Ans += (info[1] - max(0, info[0])) * info[4]
print(Ans)
*/
