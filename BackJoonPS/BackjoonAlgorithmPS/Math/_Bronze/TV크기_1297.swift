
// MARK: - TV크기 1297

// MARK: Math swift 문제풀이

/*
 import Foundation

 let arr = readLine()!.split(separator: " ").map { Double($0)! }
 let cross = arr[0]
 let ratioH = arr[1], ratioW = arr[2]
 let ratioCross = sqrt(Double((ratioH*ratioH) + (ratioW*ratioW)))
 let H = Int(arr[0] * Double(ratioH) / ratioCross)
 let W  = Int(arr[0] * Double(ratioW) / ratioCross)
 print("\(H) \(W)")
 */
