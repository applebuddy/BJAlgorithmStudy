
// MARK: - 벨트 10834

// MARK: swift 문제풀이

/*
 let N = Int(readLine()!)!
 var Ans: Double = 1
 for _ in 0..<N {
 let arr = readLine()!.split(separator: " ").map { Double($0)! }
 let a = arr[0], b = arr[1], dir = Int(arr[2])
 if dir == 0 {
     Ans /= a
     Ans *= b
 } else {
     Ans /= a
     Ans *= -b
 }
 }
 print(Ans >= 0 ? "0 \(Int(Ans))" : "1 \(Int(-1 * Ans))")
 */
