
// MARK: - 행렬 섯셈 2738

// MARK: math swift 문제풀이

/*
 let arr = readLine()!.split(separator: " ").map { Int($0)! }
 let N = arr[0], M = arr[1]
 var A = [[Int]](repeating: [Int](), count: N)
 var B = A
 for i in 0..<N {A[i] = readLine()!.split(separator: " ").map { Int($0)! }}
 for i in 0..<N {B[i] = readLine()!.split(separator: " ").map { Int($0)!}}
 for i in A.indices {for j in A[i].indices {A[i][j] += B[i][j]}}
 for i in A.indices {print(A[i].map { String($0) }.joined(separator: " "))}
 */
